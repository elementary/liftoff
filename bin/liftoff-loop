#!/bin/bash

# Load configuration
source /usr/lib/liftoff/liftoff.conf

# Start loop
for arch in `echo $target_archs` ; do for dist in `echo $target_distros` ; do liftoff ${dist} ${arch} ; done ; done
